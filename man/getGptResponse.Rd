% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getGptResponse.R
\name{getGptResponse}
\alias{getGptResponse}
\title{Request a GPT API Chat Completion}
\usage{
getGptResponse(
  token,
  base.url,
  endpoint.path = "/v1/chat/completions",
  model,
  messages,
  max.tokens = 1024,
  temperature = 1,
  stream = FALSE,
  top.p = 1,
  frequency.penalty = 0,
  presence.penalty = 0,
  stop = NULL,
  timeout = 60
)
}
\arguments{
\item{token}{Character. API token for authentication (required).}

\item{base.url}{Character. Base URL of the API endpoint (e.g., \code{"https://api.openai.com"}).}

\item{endpoint.path}{Character. The API endpoint path appended to \code{base.url}. Default is \code{"/v1/chat/completions"}.}

\item{model}{Character. Model identifier to use (e.g., \code{"gpt-35-turbo-0613"}).}

\item{messages}{List. List of messages forming the conversation history. Each message should be a named list with \code{role} and \code{content}.}

\item{max.tokens}{Integer. Maximum number of tokens to generate in the completion. Default is 1024.}

\item{temperature}{Numeric. Sampling temperature to control randomness. Must be between 0 and 2. Default is 1.}

\item{stream}{Logical. Whether to stream partial responses as they become available. Default is \code{FALSE}.}

\item{top.p}{Numeric. Nucleus sampling parameter to control diversity (0 to 1). Default is 1.}

\item{frequency.penalty}{Numeric. Penalty for repeating tokens (-2 to 2). Default is 0.}

\item{presence.penalty}{Numeric. Penalty for introducing new tokens (-2 to 2). Default is 0.}

\item{stop}{Character vector. Optional stop sequences that, when encountered, end the generation.}

\item{timeout}{Numeric. Request timeout in seconds. Default is 60.}
}
\value{
A list containing the parsed API response.
}
\description{
Sends a chat completion request to the GPT API and returns the parsed response.
}
\examples{
\dontrun{
response <- getGptResponse(
  token = "YOUR_API_TOKEN",
  base.url = "https://api.openai.com",
  model = "gpt-35-turbo-0613",
  messages = list(
    list(role = "system", content = "You are a helpful assistant."),
    list(role = "user", content = "Hello!")
  )
)
}
}
